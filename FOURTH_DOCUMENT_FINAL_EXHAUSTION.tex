\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{amsmath,amssymb,amsthm,mathtools}
\usepackage{microtype}
\usepackage{enumitem}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{xcolor}
\hypersetup{colorlinks=true,linkcolor=blue!55!black,citecolor=blue!55!black,urlcolor=blue!55!black}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{lemma}[theorem]{Lemma}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\newtheorem{definition}[theorem]{Definition}
\newtheorem{remark}[theorem]{Remark}
\newtheorem{warning}[theorem]{Warning}

\newcommand{\R}{\mathbb R}
\newcommand{\T}{\mathbb T}
\newcommand{\N}{\mathbb N}
\newcommand{\dd}{\,\mathrm d}
\newcommand{\cF}{C_F}
\newcommand{\Hdot}{\dot H}
\newcommand{\NS}{\mathrm{NS}}
\newcommand{\curl}{\operatorname{curl}}
\newcommand{\sym}{\operatorname{sym}}
\newcommand{\tr}{\operatorname{tr}}
\newcommand{\PV}{\operatorname{PV}}
\newcommand{\supp}{\operatorname{supp}}
\newcommand{\botc}{\bot}
\newcommand{\PhiIn}{\Phi_{\mathrm{in}}}
\newcommand{\PhiOut}{\Phi_{\mathrm{out}}}
\newcommand{\PhiUp}{\Phi_{\mathrm{up}}}
\newcommand{\PhiDown}{\Phi_{\mathrm{down}}}
\newcommand{\E}{\mathcal E}
\newcommand{\W}{\mathcal W}

\title{Fourth Document: Final Exhaustion Synthesis for the Wang--Navier--Stokes Physical Core\\
\large Clay formulation, exact physical identities, endpoint throat, and the status of the final exclusion step}
\author{Synthesis from the canonical repository \texttt{wang-ns-core}}
\date{Canonical source state: commit \texttt{5a078873718f6567e51ee7fc064a65ecb205f796}}

\begin{document}
\maketitle

\begin{abstract}
This fourth document assembles the exact identities and distilled deductions in the canonical three-document Wang--Navier--Stokes physical core into the contradiction architecture
\[
T_*<\infty\Longrightarrow X\vee Y,
\qquad X\Longrightarrow\bot,
\qquad Y\Longrightarrow \text{downstream physical fates}.
\]
Here $X$ is the initial-stock alternative and $Y$ is the actual canonical nonlinear boundary-inflow alternative.  The central exact throat is mode-set Kirchhoff continuity
\[
E_P(t)+D_P[s,t]+\Phi_{\mathrm{out},P}[s,t]
=E_P(s)+\Phi_{\mathrm{in},P}[s,t].
\]
The canonical endpoint theorem forces arbitrarily late, arbitrarily high hard shells; the last-entry theorem then yields $X\vee Y$, and the $\Hdot^{1/2}$ initial tail excludes $X$ at sufficiently high scales.  The $Y$ branch is reduced, using the same one-Hahn positive work law, to a universal bad-positive sidecar and then by Kirchhoff to the exhaustive stock/viscosity/outflow trichotomy.  Subsequent canonical theorems quotient pressure, SGS, strain relink, helicity recycling, source geometry, material-pair readings, and finally the traffic/material bridge.  The last commit proves by an exact quartic phase witness that the instantaneous cubic traffic quotient is not a sufficient dynamical state.

A strict logical audit is retained.  The canonical commit itself explicitly does \emph{not} claim global regularity.  In particular, the proved statement ``no fourth physical/representation fate exists outside stock/viscosity/outflow'' is not by itself the statement that each of those three fates is impossible.  Accordingly, the final sections record both (i) the exact unconditional synthesis proved by the canonical core, (ii) the full-state identities now known inside the remaining $B/S/V/O$ block, and (iii) the additional final-exclusion statement still required to turn the requested schematic
\[
T_*<\infty\Rightarrow X\vee Y,
\quad X\Rightarrow\bot,
\quad Y\Rightarrow\bot
\]
into an unconditional Clay QED.  No missing implication is silently manufactured.
\end{abstract}

\tableofcontents

\section{Problem statement and contradiction architecture}

\subsection{The Clay $\R^3$ target}

Let $\mathcal C_{\mathrm{Clay}}$ denote the smooth, divergence-free, rapidly decaying initial-data class in the $\R^3$ formulation of the Clay Navier--Stokes problem.  Thus $u_0:\R^3\to\R^3$ is smooth, satisfies
\[
\nabla\cdot u_0=0,
\]
and has the decay required in the official Clay problem description so that the standard energy and Fourier manipulations below are legitimate.  The regularity target used in this document is:

\begin{quote}
For every $u_0\in\mathcal C_{\mathrm{Clay}}$ and viscosity $\nu>0$, the corresponding incompressible three-dimensional Navier--Stokes solution exists smoothly for all $t\ge0$.
\end{quote}

The official Clay formulation is due to Fefferman; see the reference at the end.

\subsection{Primitive Navier--Stokes equation}

The primitive momentum equation is
\begin{equation}\label{eq:primitiveNS}
\partial_tu+(u\cdot\nabla)u+\nabla p=\nu\Delta u,
\qquad \nabla\cdot u=0,
\qquad u(0)=u_0.
\end{equation}
After Leray projection $\mathbb P$ onto divergence-free vector fields,
\begin{equation}\label{eq:LerayNS}
\boxed{
\partial_tu+\mathcal B(u,u)=\nu\Delta u,
\qquad \nabla\cdot u=0,
}
\end{equation}
where
\[
\mathcal B(u,u):=\mathbb P((u\cdot\nabla)u).
\]
Pressure remains physical in the primitive equation and in strain/gradient dynamics, but it is not an independent global kinetic-energy supplier after Leray projection.

The ordinary energy law is
\begin{equation}\label{eq:L2energy}
\frac12\|u(t)\|_2^2
+\nu\int_0^t\|\nabla u(s)\|_2^2\,\dd s
=\frac12\|u_0\|_2^2.
\end{equation}
We write
\[
E_*:=\|u_0\|_2^2.
\]

\subsection{Maximal smooth lifespan}

Standard local theory gives a unique maximal smooth solution
\[
u:[0,T_*)\times\R^3\to\R^3,
\qquad 0<T_*\le\infty.
\]
The contradiction architecture is exactly
\begin{equation}\label{eq:architecture}
\boxed{
T_*<\infty
\Longrightarrow X\vee Y,
\qquad
X\Longrightarrow\bot,
\qquad
Y\Longrightarrow\bot,
}
\end{equation}
which would imply
\[
T_*<\infty\Longrightarrow\bot,
\qquad\text{hence}\qquad T_*=\infty.
\]
The purpose of the remaining sections is to identify, using only canonical physical identities, precisely what has been proved toward each arrow in \eqref{eq:architecture}.

\section{Physical Fourier--helical state and the one signed work law}

\subsection{Helical modal state}

For each nonzero Fourier wavevector $k$, let $h_{k,s}$, $s\in\{+1,-1\}$, be a helical basis satisfying
\[
ik\times h_{k,s}=s|k|h_{k,s},
\qquad k\cdot h_{k,s}=0.
\]
Write
\[
\widehat u(k,t)=\sum_{s=\pm1}a_{k,s}(t)h_{k,s}.
\]
The persistent state is the full field $u$, equivalently the complete collection of helical modal amplitudes $a_{k,s}$, equivalently the vorticity $\omega=\curl u$.

For a helical mode $m=(k,s)$ define modal stock
\[
E_m(t)=|a_{k,s}(t)|^2.
\]
For a measurable mode set $A$, let $E_A(t)$ denote the corresponding integral of modal stock.  In periodic finite-Fourier diagnostics the same notation is a sum.

\subsection{Exact signed nonlinear edge work}

For one unordered helical parent orbit $x+y=z$, the exact physical child-energy work is
\begin{equation}\label{eq:edgework}
\boxed{
T_e
=4(s_x|x|-s_y|y|)
\operatorname{Re}\!\left[
\overline{a_z}\,\overline{g_e}\,a_xa_y
\right].
}
\end{equation}
The native capacity
\[
A_e=4|z|\,|a_xa_ya_z|
\]
satisfies
\[
|T_e|\le A_e,
\]
but capacity is not work, probability, a recurrence currency, or a reset budget.

The continuum unordered-parent quotient reconstructs one signed physical edge measure $dW$.  The Hahn decomposition is taken once:
\begin{equation}\label{eq:oneHahn}
\boxed{dW=dW^+-dW^-.}
\end{equation}
All later good/bad, shell, donor, contact, or service sublaws are restrictions or positive pushforwards of this already-existing law.

\subsection{Closed-triad donor kernel}

For a closed triad let
\[
T_0+T_1+T_2=0,
\qquad
P_i=[T_i]_+,
\qquad
N_i=[-T_i]_+,
\qquad
Q=\sum_iP_i=\sum_iN_i.
\]
The canonical same-time donor kernel is
\begin{equation}\label{eq:donorkernel}
\boxed{
M(i\to j)=\frac{N_iP_j}{Q}.
}
\end{equation}
Its donor marginal is $dW^-$ and its recipient marginal is the already-canonical $dW^+$.  This kernel gives same-time provenance.  It does not create temporal ancestry or generation depth.

\section{The first throat: exact mode-set Kirchhoff continuity}

\subsection{Mode-set continuity}

For every physical mode set $A$ and interval $[t_0,t_1]$, canonical donor flow gives
\begin{equation}\label{eq:modebalance}
\boxed{
E_A(t_1)+D_A+\Phi_{\mathrm{out},A}
=E_A(t_0)+\Phi_{\mathrm{in},A},
}
\end{equation}
with
\begin{equation}\label{eq:DA}
D_A
=2\nu\int_{t_0}^{t_1}
\int_{(k,s)\in A}|k|^2E_{k,s}(t)\,\dd\lambda\,\dd t.
\end{equation}
Internal nonlinear traffic cancels from the set divergence.  Equation \eqref{eq:modebalance} is stock/flow continuity, not temporal matching of energy tokens.

The absolute physical grammar is therefore
\begin{equation}\label{eq:absolutegrammar}
\boxed{
\text{final modal stock}
+\text{viscous loss}
+\text{outflow}
=
\text{earlier modal stock}
+\text{canonical boundary inflow}.
}
\end{equation}
No pressure, SGS, strain, service, material mark, or conservative relink can appear as a third kinetic-energy supplier unless it appears as an actual additional term in the exact identity; it does not.

\subsection{Native hard-state last-entry theorem}

Fix a bounded event-anchored Fourier/helicity role $P$, a time $t>0$, and
\[
e_1:=E_P(t)>0,
\qquad 0<\theta<1.
\]
If
\[
\{r<t:E_P(r)\le\theta e_1\}=\varnothing,
\]
then
\begin{equation}\label{eq:lastentryX}
E_P(0)>\theta e_1.
\end{equation}
Otherwise define
\begin{equation}\label{eq:stheta}
s_\theta
:=\sup\{r<t:E_P(r)\le\theta e_1\}.
\end{equation}
Continuity gives
\[
E_P(s_\theta)=\theta e_1,
\qquad
E_P(r)>\theta e_1
\quad (s_\theta<r\le t).
\]
Apply \eqref{eq:modebalance} on $[s_\theta,t]$:
\[
E_P(t)+D_P[s_\theta,t]+\Phi_{\mathrm{out},P}[s_\theta,t]
=E_P(s_\theta)+\Phi_{\mathrm{in},P}[s_\theta,t].
\]
Since $D_P,\Phi_{\mathrm{out},P}\ge0$,
\begin{equation}\label{eq:lastentryY}
\boxed{
\Phi_{\mathrm{in},P}[s_\theta,t]
\ge(1-\theta)e_1.
}
\end{equation}
Thus there are exactly two last-entry possibilities:
\begin{equation}\label{eq:lastentryalternative}
\boxed{
E_P(0)>\theta E_P(t)
\quad\vee\quad
\Phi_{\mathrm{in},P}[s_\theta,t]
\ge(1-\theta)E_P(t).
}
\end{equation}
This disjunction is exhaustive because the defining set in \eqref{eq:stheta} is either empty or nonempty.

\section{Critical endpoint rigidity and UV hard shells}

\subsection{Critical first moment}

Define
\begin{equation}\label{eq:C12}
C_{1/2}(t)
:=\|u(t)\|_{\Hdot^{1/2}}^2
=\int |k|\,|\widehat u(k,t)|^2\,\dd k.
\end{equation}
For hard shells
\[
P_M:=\{M/2<|k|\le M\},
\qquad
E_M(t):=\|P_Mu(t)\|_2^2,
\]
define
\begin{equation}\label{eq:mush}
\mu_{\mathrm{sh}}(t)
:=\sup_{M>0}M E_M(t).
\end{equation}
The exact shell layer-cake identity is
\begin{equation}\label{eq:layercake}
\boxed{
C_{1/2}(t)
=\int_0^\infty
\mu(R,t)\frac{\dd R}{R},
\qquad
\mu(R,t)=R\|P_{(R/2,R]}u(t)\|_2^2.
}
\end{equation}

\subsection{Radial variation collision}

The canonical radial donor flow satisfies the pointwise bound
\begin{equation}\label{eq:vrad}
\boxed{
v_{\mathrm{rad}}(t)
\le C_{\mathrm{sh}}
\sqrt{\mu_{\mathrm{sh}}(t)}
\|u(t)\|_{\Hdot^{3/2}}^2.
}
\end{equation}
Consequently
\begin{equation}\label{eq:criticaldiff}
\boxed{
\frac{\dd}{\dd t}C_{1/2}(t)
+
\bigl(2\nu-C_{\mathrm{sh}}\sqrt{\mu_{\mathrm{sh}}(t)}\bigr)
\|u(t)\|_{\Hdot^{3/2}}^2
\le0.
}
\end{equation}
Set
\begin{equation}\label{eq:cnu}
\boxed{
c_\nu:=\left(\frac{2\nu}{C_{\mathrm{sh}}}\right)^2.
}
\end{equation}
If sufficiently high shells remain below $c_\nu$ on a final interval, the critical norm remains bounded by the low-root Gronwall estimate plus \eqref{eq:criticaldiff}.  The Kenig--Koch critical criterion then gives the endpoint contrapositive used in the canonical core.

\begin{theorem}[Canonical endpoint hard-shell gate]\label{thm:endpointgate}
If $T_*<\infty$, then for every $R_0>0$ and every $t_0<T_*$ there exist $t>t_0$ and $M>R_0$ such that
\begin{equation}\label{eq:hardgate}
M E_M(t)>c_\nu.
\end{equation}
Hence one may extract
\begin{equation}\label{eq:UVseq}
t_n\to T_*^- ,
\qquad M_n\to\infty,
\qquad
M_nE_{M_n}(t_n)>c_\nu.
\end{equation}
\end{theorem}

\section{The exact disjunction $T_*<\infty\Rightarrow X\vee Y$}

For a UV hard shell from Theorem \ref{thm:endpointgate}, set
\[
\mu_n:=M_nE_{M_n}(t_n)>c_\nu.
\]
Apply the last-entry theorem to $P_{M_n}$ with
\[
\theta=\frac15.
\]
The exact alternative becomes
\begin{equation}\label{eq:XYraw}
\boxed{
M_nE_{M_n}(0)>\frac{\mu_n}{5}
\quad\vee\quad
M_n\Phi_{\mathrm{in},M_n}\ge\frac{4\mu_n}{5}.
}
\end{equation}

\begin{definition}[The two first-throat alternatives]
For sufficiently large $n$, define
\begin{align}
X_n&:\quad
M_nE_{M_n}(0)>\frac{\mu_n}{5},\label{eq:defX}\\
Y_n&:\quad
M_n\Phi_{\mathrm{in},M_n}\ge\frac{4\mu_n}{5}.
\label{eq:defY}
\end{align}
We write $X$ and $Y$ for the corresponding asymptotic alternatives along the endpoint UV sequence.
\end{definition}

Then
\begin{equation}\label{eq:TtoXY}
\boxed{
T_*<\infty\Longrightarrow X\vee Y.
}
\end{equation}
This is the first rigid throat of the architecture.  It is not a heuristic physical classification; it is a direct disjunction from the exact identity \eqref{eq:modebalance}.

\section{Exclusion of $X$: initial critical tail vanishes}

Since $u_0\in\Hdot^{1/2}$,
\begin{equation}\label{eq:tailvanish}
M E_M(0)\longrightarrow0
\qquad (M\to\infty).
\end{equation}
Indeed, on $M/2<|k|\le M$,
\[
M|\widehat u_0(k)|^2
\le2|k|\,|\widehat u_0(k)|^2,
\]
so
\[
M E_M(0)
\le
2\int_{M/2<|k|\le M}
|k|\,|\widehat u_0(k)|^2\,\dd k
\to0.
\]
But $X_n$ would imply
\[
M_nE_{M_n}(0)
>\frac{\mu_n}{5}
>\frac{c_\nu}{5},
\]
contradicting \eqref{eq:tailvanish} for sufficiently large $n$.

\begin{proposition}[Exclusion of initial stock]\label{prop:Xbot}
Along the endpoint UV sequence,
\begin{equation}\label{eq:Xbot}
\boxed{X\Longrightarrow\bot.}
\end{equation}
Consequently
\begin{equation}\label{eq:TtoY}
\boxed{
T_*<\infty\Longrightarrow Y,
}
\end{equation}
and in particular, eventually
\begin{equation}\label{eq:Yquantum}
\boxed{
M_n\Phi_{\mathrm{in},M_n}
\ge\frac{4c_\nu}{5}.
}
\end{equation}
\end{proposition}

\section{The $Y$ branch: same-Hahn $G/B_{\mathrm{bad}}$ refinement}

\subsection{Good and endpoint-complement work are restrictions of the same $dW^+$}

The actual boundary inflow in \eqref{eq:Yquantum} is a restriction of canonical $dW^+$.  To make the native signed-good reader explicit, for a forward edge $x+y=z$ put
\[
\ell_e:=\log_+\frac{|z|}{\max(|x|,|y|)},
\qquad
A_e:=4|z|\,|a_xa_ya_z|,
\]
\[
J_e:=\ell_e\,|s_x|x|-s_y|y||\,\frac{|g_e|}{|z|},
\qquad
c_e:=\operatorname{sgn}(s_x|x|-s_y|y|)
\frac{\Re(\overline{a_z}\,\overline{g_e}a_xa_y)}{|g_ea_xa_ya_z|},
\]
with $c_e=0$ on a zero interaction factor.  Let $J_*$ be the sign-exhausted global supremum of $J_e$ on normalized admissible edge geometry and put
\[
r_e:=\frac{J_e}{J_*}c_e,
\qquad \eta_0:=10^{-4}.
\]
The physical edge formula gives the exact registration identity
\begin{equation}\label{eq:nativeEfficiency}
\boxed{T_e\ell_e=A_eJ_ec_e=A_eJ_*r_e.}
\end{equation}
Only after the one Hahn-positive law exists define
\[
G:=\{T_e>0:r_e>1-\eta_0\},
\qquad
B_{\mathrm{bad}}:=\{T_e>0:r_e\le1-\eta_0\}.
\]
These are complementary measurable restrictions of the same $dW^+$, not new work laws.  The reader $r_e$ is not an owner, probability, clock or NS-created threshold.  Moreover $B_{\mathrm{bad}}$ must not be confused with $B_{\mathrm{crit}}$ below, the minority-helicity positive-critical branch determined by the exact critical-moment identity \eqref{eq:minoritymoment}.  The full-state $B/S/V/O$ frontier uses $B=B_{\mathrm{crit}}$.

Thus
\[
\Phi_{\mathrm{in},M}
=
\Phi_{\mathrm{in},M}^{G}
+
\Phi_{\mathrm{in},M}^{B_{\mathrm{bad}}},
\]
with no new Hahn decomposition.  Therefore
\begin{equation}\label{eq:GBhalf}
\boxed{
M\Phi_{\mathrm{in},M}^{G}\ge\frac{2c_\nu}{5}
\quad\vee\quad
M\Phi_{\mathrm{in},M}^{B_{\mathrm{bad}}}\ge\frac{2c_\nu}{5}.
}
\end{equation}

\subsection{Signed-good geometry from the exact signed-root identities}

For the endpoint spine, no efficiency coordinate is needed after the signed-good restriction has been made.  A signed-good positive-work atom can, after a simultaneous reversal of all helicity signs if necessary, be written with ordered signed roots
\[
\alpha=-r<\beta=d<\gamma=c,
\]
where $c=|k_c|$ is the main positive recipient, $d=|k_d|$ is the unique energy donor, and $r$ is the simultaneous opposite-helicity side recipient.  The certified physical support window is
\begin{equation}\label{eq:58geom}
\boxed{
\frac35<\frac dc<\frac58,
\qquad
\frac35<\frac rc<\frac58.
}
\end{equation}
The two exact closed-triad invariants
\[
T_\alpha+T_\beta+T_\gamma=0,
\qquad
\alpha T_\alpha+\beta T_\beta+\gamma T_\gamma=0
\]
leave a one-dimensional work line.  Writing $Z:=\sum_i x_i^2T_i$, the $Z>0$ orientation gives
\begin{equation}\label{eq:triadRootWorks}
\boxed{
T_{-r}=\frac{Z}{(d+r)(c+r)},\qquad
T_d=-\frac{Z}{(d+r)(c-d)},\qquad
T_c=\frac{Z}{(c+r)(c-d)}.
}
\end{equation}
Hence $d$ is the unique donor and $-r,c$ are the two positive recipients.  In particular the side/main ratio is the exact identity
\begin{equation}\label{eq:sideRatioExact}
\frac{W_{\mathrm{side}}}{W_{\mathrm{child}}}
=\frac{T_{-r}}{T_c}
=\frac{c-d}{d+r}
>\frac{3/8}{5/4}=\frac3{10}.
\end{equation}
Therefore, on the deterministic $5/8$ control-volume grid $C=(5R/8,R]$, a $G$ child in $C$ has its unique donor below the lower face and is genuine boundary inflow.  For the original dyadic endpoint shell $P_M$, no such inference is needed: \eqref{eq:GBhalf} has already restricted to actual boundary inflow.  The simultaneous side recipient is a consequence of the same physical triad, not a second event or a second Hahn split.

At a first contact $ME_M=\mu_*$, with
\begin{equation}\label{eq:mustar}
\mu_*:=\min\left(\frac{c_\nu}{2},\frac{\nu^2}{5184\pi}\right),
\end{equation}
the exact differential mode balance and shell spectral gap give
\[
\phi_{\mathrm{in},M}\ge\frac{\nu M\mu_*}{2}.
\]
If $G$ owns at least half this rate, the whole-band work estimate yields a lower hard state
\begin{equation}\label{eq:Gfertility}
\boxed{
R\le\frac58M,
\qquad
R E_R\ge2\mu_*.
}
\end{equation}
That lower state is either already above $\mu_*$ at $t=0$ or has a strictly earlier first $\mu_*$ contact.  A pure-$G$ state descent is finite because every step contracts scale by at most $5/8$ while finite total energy gives
\[
R\ge\frac{\mu_*}{E_*}.
\]
Thus pure $G$ cannot be an independent infinite-depth escape road.

\subsection{Universal bad-positive sidecar}

Equation \eqref{eq:sideRatioExact} is the exact same-time side-recipient identity.  Therefore either branch of \eqref{eq:GBhalf} contains a canonical bad-positive sublaw: the $B_{\mathrm{bad}}$ branch is already such a restriction of the original boundary inflow, while the $G$ branch carries the simultaneous nonforward side recipient.  Quantitatively,
\begin{equation}\label{eq:badquantum}
\boxed{
\text{every sufficiently large UV birth carries bad-positive work of child-scale amount}
\ge\frac{3c_\nu}{25}.
}
\end{equation}
This is real recipient kinetic-energy work.  ``Transfer loss'' denotes only a recursion/proof-graph fate; it is not a physical energy sink.

\section{The second throat: bad sidecar $\Rightarrow S\vee V\vee O$}

For a $G$ birth, the side recipient has helicity opposite to the unique donor and its radius lies in
\[
\left(\frac{3M}{10},\frac{5M}{8}\right].
\]
Cover this interval by the two hard annuli $P_{M/2}=(M/4,M/2]$ and $P_M=(M/2,M]$, and then by the two helicity signs.  Let
\[
q:=\max_A N_A\Phi_{\mathrm{in},A}^{\mathrm{side}}
\]
over these four fixed Fourier--helicity cells, where $N_A=M/2$ on the lower radial annulus and $N_A=M$ on the upper one.  The exact bookkeeping is
\[
M\Phi_{\mathrm{side}}
\le 4q+2q=6q.
\]
On the $G$ branch, \eqref{eq:GBhalf}, \eqref{eq:badquantum}, and the exact ratio \eqref{eq:sideRatioExact} give $M\Phi_{\mathrm{side}}\ge3c_\nu/25$, hence $q\ge c_\nu/50$.  On the $B_{\mathrm{bad}}$ branch the original shell is already a boundary inflow; splitting only its two helicities gives the stronger $q\ge c_\nu/5$.  Thus in both cases there is a fixed helicity-resolved annulus $A_N$ with the safe universal quantum
\begin{equation}\label{eq:sidecarIn}
\boxed{
N\Phi_{\mathrm{in},A}^{\mathrm{side}}
\ge\frac{c_\nu}{50}.
}
\end{equation}
No helicity sign is silently assumed fixed before this covering.

Applying exact mode-set continuity to $A_N$ and using only nonnegativity of stock, viscosity and outflow gives the canonical trichotomy
\begin{equation}\label{eq:SVO}
\boxed{
N E_A(t_1)\ge\frac{c_\nu}{150}
\quad\vee\quad
N D_A\ge\frac{c_\nu}{150}
\quad\vee\quad
N\Phi_{\mathrm{out},A}\ge\frac{c_\nu}{150}.
}
\end{equation}
Define
\begin{align*}
S&:=\left\{N E_A(t_1)\ge\frac{c_\nu}{150}\right\},\\
V&:=\left\{N D_A\ge\frac{c_\nu}{150}\right\},\\
O&:=\left\{N\Phi_{\mathrm{out},A}\ge\frac{c_\nu}{150}\right\}.
\end{align*}
Then the canonical core proves
\begin{equation}\label{eq:YtoSVO}
\boxed{
Y\Longrightarrow S\vee V\vee O.
}
\end{equation}

\subsection{Why this trichotomy is ontologically exhaustive}

Equation \eqref{eq:modebalance} contains no fourth sidecar-energy fate.  Once the inflow quantum has entered $A_N$, the only terms available to balance it are modal stock, viscous loss, earlier stock, and outflow.  In the canonical sidecar birth construction the earlier-stock face has already been absorbed into the birth/last-entry setup, leaving exactly \eqref{eq:SVO}.  Thus no pressure, SGS, strain, service, material, relink, helicity-action, or transfer-loss wallet can be appended as a fourth kinetic-energy fate.

This is the precise sense in which the late commit sequence destroys the suspicion of a hidden fourth fate $M$:
\begin{equation}\label{eq:noM}
\boxed{
Y\Longrightarrow S\vee V\vee O,
\qquad
\text{with no independent fourth kinetic-energy fate }M.
}
\end{equation}

\section{Outflow and recycling quotient}

Embed the helicity-resolved annulus $A_N$ into the full low ball
\[
L_{2N}:=\{|k|\le2N\},
\]
with both helicities.  Any actual $A_N$-outflow donor atom has exactly two possibilities:

\begin{enumerate}[label=(\roman*)]
\item recipient radius $\le2N$: the transfer is internal traffic of $L_{2N}$ and cancels exactly from the low-ball mode-set divergence;
\item recipient radius $>2N$: the transfer is a restriction of the already-canonical true upward radial flux $\Phi_{\mathrm{up}}(2N)$.
\end{enumerate}
Hence
\begin{equation}\label{eq:Oquotient}
\boxed{
O\Longrightarrow
\text{internal circulation}
\quad\vee\quad
\text{true radial upward crossing}.
}
\end{equation}
There is no third recycling owner.

With signed radius
\[
x=s|k|,
\]
closed-triad energy and helicity conservation give
\begin{equation}\label{eq:signedradiuscons}
\sum_iT_i=0,
\qquad
\sum_ix_iT_i=0.
\end{equation}
The donor kernel therefore has zero total signed-radius displacement, and the global variation satisfies
\begin{equation}\label{eq:VxVrho}
\boxed{V_x\le2V_\rho.}
\end{equation}
Helicity-flip recycling and same-helicity radial recycling are thus controlled by the same radial-variation ledger; no separate helicity-recycling currency exists.

\section{Critical production is exactly minority-helicity bad work}

For a homochiral closed triad,
\[
\sum_i\rho_iT_i=0.
\]
For a heterochiral triad with minority-helicity root $m$,
\begin{equation}\label{eq:minoritymoment}
\boxed{
\sum_i\rho_iT_i=2\rho_mT_m.
}
\end{equation}
After integration,
\begin{equation}\label{eq:criticalminority}
\boxed{
C_{1/2}(t)+2\nu D_{3/2}[0,t]
=C_{1/2}(0)+2W_{\min}^{\mathrm{signed}}[0,t].
}
\end{equation}
If $T_m>0$, the two interaction parents have the same helicity; the edge is never signed-good.  Define $B_{\mathrm{crit}}$ to be this positive minority-helicity restriction of the already-canonical $dW^+$.  Then the exact identity \eqref{eq:minoritymoment}, not an efficiency reader, gives
\begin{equation}\label{eq:allpositivecriticalB}
\boxed{
\text{every positive nonlinear creator of critical stock is canonical minority-helicity }B_{\mathrm{crit}}\text{ work}.
}
\end{equation}
This $B_{\mathrm{crit}}$ is the symbol abbreviated to $B$ in the final $B/S/V/O$ frontier.  It is distinct from the endpoint complement $B_{\mathrm{bad}}=G^c$ used only to split the incoming positive law.

\section{Signed curl, enstrophy source, and Betchov identity}

Put
\[
x_i=s_i\rho_i,
\qquad
Z_{\mathrm{tri}}:=\sum_i\rho_i^2T_i.
\]
Order distinct signed roots
\[
\alpha<\beta<\gamma.
\]
The exact cyclic law gives
\begin{align}
T_\alpha&=\frac{Z}{(\beta-\alpha)(\gamma-\alpha)},\label{eq:Talpha}\\
T_\beta&=-\frac{Z}{(\beta-\alpha)(\gamma-\beta)},\label{eq:Tbeta}\\
T_\gamma&=\frac{Z}{(\gamma-\alpha)(\gamma-\beta)}.\label{eq:Tgamma}
\end{align}
Thus the canonical donor graph is exactly the path
\[
\alpha\;--\;\beta\;--\;\gamma,
\]
and radial traffic is the fold $\rho=|x|$ of this signed-curl path.

Let
\begin{equation}\label{eq:Qdef}
Q(t):=\int_{\R^3}\omega\cdot S\omega\,\dd x,
\qquad
S:=\frac12(\nabla u+\nabla u^T).
\end{equation}
Root marking of the same triad measure gives
\begin{equation}\label{eq:ZtoQ}
\boxed{
C_F\int Z_{\mathrm{tri}}\,\dd\Lambda_{\mathrm{tri}}=2Q.
}
\end{equation}
For $A=\nabla u=S+\Omega$, with $\Omega\omega=0$, trace-free $3\times3$ algebra plus the Piola identity gives
\begin{equation}\label{eq:Betchov}
\boxed{
Q
=-4\int\det S\,\dd x
=-\frac43\int\tr(S^3)\,\dd x.
}
\end{equation}
The exact $H^1$ stock law is
\begin{equation}\label{eq:H1stock}
\boxed{
\frac12\|\nabla u(t)\|_2^2
+\nu\int_0^t\|\Delta u(s)\|_2^2\,\dd s
=
\frac12\|\nabla u_0\|_2^2
+\int_0^tQ(s)\,\dd s.
}
\end{equation}
Therefore every finite singular endpoint satisfies, for every $t_0<T_*$,
\begin{equation}\label{eq:Qunbounded}
\boxed{
\sup_{t_0<t<T_*}\int_{t_0}^tQ(s)\,\dd s=+\infty.
}
\end{equation}
This is a necessary endpoint source law.  It is not an atomwise identification of a UV inflow event with positive stretching.

\section{Pressure quotient and pressure-free vorticity state}

Set
\[
g:=-\Delta p=|S|^2-\frac12|\omega|^2,
\qquad
H_0:=\nabla^2p+\frac g3I.
\]
For $k\ne0$,
\[
\widehat H_0(k)
=\left(\frac I3-\widehat k\otimes\widehat k\right)\widehat g(k),
\qquad
\widehat S:\widehat H_0=0.
\]
The projected strain equation is
\[
\partial_tS=\sym\nabla N+\nu\Delta S,
\qquad
N=-\mathbb P(u\cdot\nabla u)=\mathbb P(u\times\omega).
\]
Thus pressure is an exact geometric reconstruction, not an independent modal $H^1$ source.

The closed pressure-free vorticity equation is
\begin{equation}\label{eq:vorticity}
\boxed{
\partial_t\omega+(u[\omega]\cdot\nabla)\omega
=(\omega\cdot\nabla)u[\omega]+\nu\Delta\omega,
\qquad
u[\omega]=\curl(-\Delta)^{-1}\omega.
}
\end{equation}
Equivalently,
\[
(\omega\cdot\nabla)u=S[\omega]\omega.
\]
Pressure is reconstructed afterward as the longitudinal Hodge completion.

For the flow map $X(a,t)$, with deformation gradient $F=\nabla_aX$,
\begin{equation}\label{eq:Cauchydefect}
\boxed{
\frac{\dd}{\dd t}
\bigl[F^{-1}\omega(X,t)\bigr]
=\nu F^{-1}\Delta\omega(X,t),
\qquad
\det F=1.
}
\end{equation}

\section{Biot--Savart material-pair representation}

Biot--Savart gives the exact global source representation
\begin{equation}\label{eq:BSQ}
\boxed{
Q
=\frac{3}{8\pi}
\iint
\frac{((\omega_y-\omega_x)\cdot h)
((\omega_x\times\omega_y)\cdot h)}{|h|^5}
\,\dd x\,\dd y,
\qquad h=y-x.
}
\end{equation}
For material particles $x=X(a,t)$ and $y=X(b,t)$, define
\begin{equation}\label{eq:LCk}
L=(\omega_y-\omega_x)\cdot h,
\qquad
C=(\omega_x\times\omega_y)\cdot h,
\qquad
K=\frac{LC}{|h|^5}.
\end{equation}
Let
\[
\overline A
:=\int_0^1\nabla u(x+\theta h)\,\dd\theta,
\qquad
E_x=A_x-\overline A,
\qquad
E_y=A_y-\overline A.
\]
Then
\[
\dot h=\overline A h,
\qquad
\tr\overline A=0,
\]
and the exact chirality law is
\begin{align}
\dot C
&=\det(E_x\omega_x,\omega_y,h)
+\det(\omega_x,E_y\omega_y,h)\notag\\
&\quad
+\nu\bigl[
\det(\Delta\omega_x,\omega_y,h)
+\det(\omega_x,\Delta\omega_y,h)
\bigr].
\label{eq:Cdot}
\end{align}
Common incompressible affine deformation cancels exactly from $\dot C$; only material segment gradient-curvature and viscosity can leave the chirality-null manifold.  This is an exact representation of the same full state/source, not a new owner of UV traffic.

\section{Anti-theorem against an atomwise traffic-to-stretching owner}

The canonical core constructs two homochiral triad witnesses with the same child, the same radial cut, and the same positive boundary-crossing mass but opposite signs of $Z_{\mathrm{tri}}$.  Therefore
\begin{equation}\label{eq:noatommap}
\boxed{
\text{UV boundary-inflow atom}
\not\longmapsto
\text{a canonically determined }Q^+\text{ atom}.
}
\end{equation}
This forbids an atomwise first-contact-to-positive-stretching assignment.  It does not destroy either exact representation; it states that they are not related by the proposed owner map.

\section{The final quartic phase law: representation frontier exhaustion}

\subsection{The finite-Fourier witness}

On the torus, take the signed copies of the two divergence-free active families
\[
A=\{(-1,-1,-1),(0,1,1),(1,0,0)\},
\]
\[
B=\{(-7,1,-3),(3,0,2),(4,-1,1)\}.
\]
Use rational divergence-free carriers $(k_2,-k_1,0)$ on the first two roots of each family and multiply the third root by $i$, with the usual reality relation on negative modes.  There is no mixed active closed triad.

Translate only family $B$ by
\[
\delta=(\pi,0,0),
\]
so that the $B$-family coefficient at $k$ is multiplied by $(-1)^{k_1}$.  Then:
\begin{enumerate}[label=(\roman*)]
\item every modal stock is unchanged;
\item every active canonical cubic work is unchanged, because each surviving active work triple is internal to one family and its translation phases multiply to one;
\item the normalized stretching source remains
\[
\overline Q=-980.
\]
\end{enumerate}
Nevertheless the active support contains the mixed quartet
\begin{equation}\label{eq:mixedquartet}
(1,0,0)+(0,-1,-1)+(3,0,2)+(-4,1,-1)=0,
\end{equation}
whose $B$ subtotal has odd translation parity.  Exact rational Fourier algebra gives
\begin{equation}\label{eq:QdotWitness}
\boxed{
\dot{\overline Q}_{\delta}
-\dot{\overline Q}_0
=\frac{87152}{55}\ne0,
}
\end{equation}
independently of $\nu$.

\begin{theorem}[Canonical traffic/material anti-correspondence]\label{thm:antiCorrespondence}
Full instantaneous modal stock, all active canonical cubic work, and the scalar $Q$ do not determine the next true Navier--Stokes source jet $\dot Q$.  The cubic traffic quotient has forgotten relative phase information still present in the full state and detected by a quartic time jet.
\end{theorem}

\begin{corollary}[Representation frontier exhaustion]\label{cor:repExhaustion}
There is no missing canonical traffic-to-material-pair spacetime bridge that can be recovered from the instantaneous cubic traffic quotient.  Fourier traffic, signed-curl $Z_{\mathrm{tri}}$, Biot--Savart pair geometry, and material secant laws are exact readings of the same full $u/\omega$, but the lossy readers are not sufficient dynamical states for one another.
\end{corollary}

The importance of the final commit is therefore not a new regularity estimate.  It destroys the suspicion that a hidden ``fourth owner'' is waiting in a bridge between the traffic and material representations.  If one needs future dynamical information, one must retain the full primitive state rather than invent a missing owner in the quotient.

\section{What the canonical core proves unconditionally}

Collecting the certified endpoint and traffic statements gives the following theorem.

\begin{theorem}[Canonical endpoint-exhaustion synthesis]\label{thm:canonicalSynthesis}
For a maximal smooth solution from Clay-admissible initial data, if $T_*<\infty$, then there exist arbitrarily late, arbitrarily high UV hard shells for which the following implications hold:
\begin{equation}\label{eq:canonicalChain1}
T_*<\infty
\Longrightarrow
X\vee Y,
\end{equation}
\begin{equation}\label{eq:canonicalChain2}
X\Longrightarrow\bot,
\end{equation}
and hence
\begin{equation}\label{eq:canonicalChain3}
T_*<\infty\Longrightarrow Y.
\end{equation}
Moreover,
\begin{equation}\label{eq:canonicalChain4}
Y\Longrightarrow S\vee V\vee O,
\end{equation}
where $S,V,O$ are exactly the sidecar stock, genuine viscous loss, and actual outflow fates in \eqref{eq:SVO}.  There is no independent fourth kinetic-energy fate.  The outflow road is further quotiented into internal circulation or true upward radial flux; helicity recycling has no independent ledger; every positive nonlinear critical-stock creator is minority-helicity $B_{\mathrm{crit}}$ work; pressure and material pair geometry are derived representations of the same full pressure-free vorticity state; and the final quartic phase witness rules out a hidden traffic/material bridge.
\end{theorem}

\begin{proof}
The endpoint gate is Theorem \ref{thm:endpointgate}.  Applying the exact last-entry theorem at $\theta=1/5$ gives \eqref{eq:XYraw}, hence \eqref{eq:canonicalChain1}.  Initial $\Hdot^{1/2}$ tail vanishing gives Proposition \ref{prop:Xbot} and therefore \eqref{eq:canonicalChain3}.  Restricting the same canonical $dW^+$ gives the $G/B_{\mathrm{bad}}$ alternative; a $B_{\mathrm{bad}}$-dominant birth is already positive boundary inflow, while a $G$ birth carries the simultaneous nonforward side recipient \eqref{eq:sideRatioExact}.  This gives the universal bad sidecar \eqref{eq:badquantum}.  Helicity-annulus covering gives \eqref{eq:sidecarIn}; exact Kirchhoff continuity gives \eqref{eq:SVO}, hence \eqref{eq:canonicalChain4}.  The outflow and signed-radius quotients are \eqref{eq:Oquotient} and \eqref{eq:VxVrho}; critical production is \eqref{eq:criticalminority}; pressure-free closure is \eqref{eq:vorticity}; and representation frontier exhaustion is Theorem \ref{thm:antiCorrespondence} and Corollary \ref{cor:repExhaustion}.  No additional owner, budget, Hahn split, genealogy, or traffic/material bridge is introduced.
\end{proof}

\section{Why ``no fourth fate'' is not yet $Y\Rightarrow\bot$}

This section is logically essential.  The statement
\[
Y\Longrightarrow S\vee V\vee O
\]
with no fourth fate is a complete \emph{classification of immediate physical fates}.  To infer
\[
Y\Longrightarrow\bot,
\]
one must additionally prove
\begin{equation}\label{eq:allleafbot}
S\Longrightarrow\bot,
\qquad
V\Longrightarrow\bot,
\qquad
O\Longrightarrow\bot,
\end{equation}
or prove an equivalent well-founded exhaustion in which every possible re-entry into the same grammar terminates in a contradictory leaf.  The canonical files at commit \texttt{5a07887} do not contain such a theorem.  Three elementary scale checks show why it cannot be inserted merely by re-reading \eqref{eq:SVO}.

\subsection{The stock leaf is not itself contradictory}

The sidecar stock lower bound is
\[
N E_A(t_1)\ge\frac{c_\nu}{100},
\]
which is only
\[
E_A(t_1)\ge\frac{c_\nu}{100N}.
\]
For a sparse UV sequence $N_j\to\infty$, a family of such lower bounds is compatible with finite ordinary energy because, for example, $\sum_jN_j^{-1}$ can converge if $N_j$ grows sufficiently rapidly.  A stock leaf is therefore a genuine hard state that must be re-entered into the exact stock/inflow grammar; it is not a contradiction merely because its scale is large.

\subsection{The viscous leaf is not itself contradictory}

The sidecar viscous lower bound is
\[
N D_A\ge\frac{c_\nu}{100},
\]
so only
\[
D_A\ge\frac{c_\nu}{100N}.
\]
The global energy law controls the unweighted sum/integral of genuine dissipation, not a reusable scale-normalized amount $ND_A$.  On sparse scales, lower bounds of order $1/N_j$ are compatible with finite total dissipation.  The canonical core itself repeatedly warns that an $O(1)$ normalized payment per critical event cannot be declared a finite event-count budget without an actual globally bounded resource or exact disjointness theorem in matching units.

\subsection{The outflow leaf is not itself contradictory}

The outflow bound
\[
N\Phi_{\mathrm{out},A}\ge\frac{c_\nu}{100}
\]
means
\[
\Phi_{\mathrm{out},A}\ge\frac{c_\nu}{100N}.
\]
The outflow quotient correctly says that this is internal low-ball circulation or true upward radial crossing.  It does not state that true upward crossing is impossible.  Indeed true upward flux is exactly the already-canonical mechanism that can supply later high modes.  Quotienting the owner closes the ontology; it does not by itself forbid the physical flow.

\subsection{The later source theorems remain necessary-route theorems}

The canonical source diagnostics reduce sparse late UV first contacts to genuine endpoint roads such as infinite inward radial action or infinite Leray--Lamb action.  The latter satisfies
\[
N=-\mathbb P(u\cdot\nabla u)=\mathbb P(u\times\omega),
\qquad
\langle N,-\Delta u\rangle=Q.
\]
Finiteness of Lamb $L_t^2L_x^2$ action would bound $H^1$ and contradict a finite singular endpoint, but divergence of Lamb action is not itself a contradiction.  Likewise, unbounded cumulative $Q$ in \eqref{eq:Qunbounded} is a necessary singular source law, not a forbidden event.

\begin{warning}[Exact logical boundary of the canonical commit]\label{warn:boundary}
The canonical commit proves
\[
T_*<\infty\Longrightarrow Y\Longrightarrow S\vee V\vee O
\]
and proves that no hidden fourth owner or traffic/material bridge is available.  It does \emph{not} prove \eqref{eq:allleafbot}.  Therefore ``no fourth fate'' cannot be substituted for ``all three listed fates are impossible.''
\end{warning}

This is not a request to reopen an ontology frontier.  It is only the distinction between an exhaustive classification theorem and an exclusion theorem.


\section{Exact progress inside the remaining $B/S/V/O$ block}

The representation frontier is no longer the mathematical unknown.  The surviving problem is a direct full-state statement about how the already-canonical sidecar roads can re-enter the true Navier--Stokes dynamics.  Here $B$ denotes $B_{\mathrm{crit}}$, the existing minority-helicity positive-critical work branch, and is not the endpoint complement $B_{\mathrm{bad}}$ or a fourth sidecar fate.

\subsection{Mother-law force triangle and signed-curl action profile}
Put
\[
N=-\mathbb P(u\cdot\nabla u)=u_t+\nu\Lambda^2u.
\]
For every fixed Fourier--helicity projector $P$, if
$W_P=2\langle Pu,PN\rangle$ is the actual net nonlinear boundary work, exact Hilbert algebra gives
\[
\nu W_P
=\|\Lambda^{-1}PN\|_2^2
-\|\Lambda^{-1}Pu_t\|_2^2
+\nu^2\|\Lambda Pu\|_2^2,
\]
while the same set obeys Kirchhoff continuity
\[
\frac d{dt}\|Pu\|_2^2+2\nu\|\Lambda Pu\|_2^2=W_P.
\]
The usual critical-action identity is the radial layer-cake of this fixed-set force triangle.

Now write the helical signed-curl coordinate $x=s|k|$ and push actual modal work to $dW_t(x)$.  Energy and helicity conservation give
\[
\int dW_t=0,
\qquad
\int x\,dW_t=0.
\]
Define
\[
\Psi(a,t)=\int (x-a)_+\,dW_t(x),
\qquad M_a=|\curl-a|,
\]
and the quadratic full-state profile
\[
\mathcal A(a,t)
:=\|M_a^{1/2}\Lambda^{-1}N\|_2^2
-\|M_a^{1/2}\Lambda^{-1}u_t\|_2^2
+\nu^2\|M_a^{1/2}\Lambda u\|_2^2.
\]
Then
\[
\boxed{\mathcal A(a,t)=2\nu\Psi(a,t)}.
\]
Hence profile slope is actual signed-curl boundary work and profile curvature is actual modal nonlinear work, distributionally.  Moreover
\[
\mathcal P_{1/2}^{NL}=2\Psi(0),
\qquad
Q=\int_{\mathbb R}\Psi(a)\,da.
\]
Thus critical production and vortex stretching are height/area readings of one actual work/action profile, not independent mechanisms.

\subsection{Neutral-helicity tail and forced radial sorter}
On a smooth finite-energy $\mathbb R^3$ state with positive critical production, the positive component of $\Psi$ containing zero has a maximum $a_*$.  The corresponding single-helicity tail $P=P_{\sigma,R_*}$ satisfies, with $U=Pu$,
\[
2\langle U,PN\rangle=0,
\qquad
H:=2\langle\Lambda U,PN\rangle
\ge \frac12\mathcal P_{1/2}^{NL}>0.
\]
Thus the tail has zero nonlinear kinetic-energy source but positive helicity/critical transfer.  Put
\[
E=\|U\|_2^2,
\qquad
\mu=\frac{\langle\Lambda U,U\rangle}{E},
\qquad
V=(\Lambda-\mu)U,
\qquad
S=\|V\|_2^2.
\]
Then $S>0$ and exact orthogonal projection gives
\[
\boxed{
PN=\frac{H}{2S}(\Lambda-\mu)U+N_\perp,
\qquad
N_\perp\perp U,
\qquad
N_\perp\perp V.}
\]
Hence all kinetic-neutral / critical-positive first-moment hardening is carried by one forced radial sorter.  The surviving phase, polarization, same-heat and higher-shape complexity lies in the moment-orthogonal remainder $N_\perp$.

Writing
\[
\Lambda^2U=\frac{D_2}{E}U+\beta V+Z,
\qquad Z\perp U,V,
\qquad \beta=\frac{D_3-\mu D_2}{S}>2R_*,
\]
true Navier--Stokes gives
\[
U_t
=-\nu\frac{D_2}{E}U
+\left(\frac{H}{2S}-\nu\beta\right)V
+(N_\perp-\nu Z).
\]
Thus nonlinearity hardens and viscosity softens along the same actual radial-shape direction.  At a nondegenerate action maximum, the same-helicity tail flux reverses from shallower donor to deeper recipient across the neutral radius.

Finally set $G=\nabla\times N$.  Then
\[
G=\omega_t-\nu\Delta\omega=S\omega-(u\cdot\nabla)\omega,
\]
with exact pressure-free evolution
\[
G_t-\nu\Delta G=[G,u]+[\omega,N]-2\nu\sum_j[\partial_j\omega,\partial_j u].
\]
The higher work curvature is governed by the genuine field $F_N=N_t-\nu\Delta N$; there is no unnamed repair mechanism.

\subsection{Pressure reflection and triad silent gates}
Let $M=S^2+\Omega^2$ and $R=M+\nabla^2p=-(D_tS-\nu\Delta S)$.  Piola gives for $q\ne0$, $e=q/|q|$,
\[
\widehat R(q)
=\widehat M(q)-2\langle\widehat M(q),e\otimes e\rangle_F e\otimes e,
\]
so $|\widehat R(q)|_F=|\widehat M(q)|_F$: pressure is a modewise geometric reflection, not an independent source magnitude.

For a selected closed helical triad, write its canonical works as $T_i=c_iZ_\tau$ and let $\mathfrak Z_\tau=Z_\tau+iJ_\tau$ be the corresponding complex interaction amplitude.  If $E_i=|a_i|^2$, define
\[
B_\tau
=c_\alpha E_\beta E_\gamma
+c_\beta E_\gamma E_\alpha
+c_\gamma E_\alpha E_\beta.
\]
The exact traffic jet has the form
\[
\dot Z_\tau+\nu\Sigma_\tau Z_\tau
=\frac{|\Lambda_\tau|^2}{2}B_\tau+\mathcal M_\tau,
\]
where $\mathcal M_\tau$ is the mixed full-state phase interference.  At an active silent gate $Z_\tau=0$, direct viscosity does not rotate phase and $\dot Z_\tau=J_\tau\Omega_\tau$ with $\Omega_\tau$ the sum of the three actual modal angular velocities.

\subsection{Dangerous $O/B$ geometry and full-state leakage}
For a true far-UV low-donor triad whose two high roots have opposite helicity, the low root is the signed middle root, both high roots are recipients, $Z_\tau>0$, the radial flux profile is nonnegative through every cut, and the critical production is positive minority-helicity $B_{\mathrm{crit}}$ work.  Same-helicity high pairs instead form a radial loop with a farther high donor and have nonpositive critical production.

On three distinct signed roots, any nonlinear work vector conserving both energy and helicity is proportional to the canonical cyclic vector $(c_\alpha,c_\beta,c_\gamma)$; the same is true for any time jet whose work support remains on those roots.  Thus a different radial compensation needs additional signed-root work support, although tangential phase compensation remains invisible to those two scalar moments.

The helical atom law remains exact, but a full-state conclusion must sum both parent helicities.  For full divergence-free Fourier coefficients $U=\widehat u(p)$, $V=\widehat u(q)$ at $K=p+q$, the pair-plane decomposition gives the exact full-polarization Pythagoras
\[
|N_K^{(p,q)}|^2
=|p\times q|^2\left(
|BC+DA|^2
+\frac{(|q|^2-|p|^2)^2}{|K|^2}|BD|^2
\right).
\]
Consequently unequal-radius full-pair silence is precisely common-normal shear; equal-radius pairs have the wider null manifold $BC+DA=0$, so equiradial mirror cancellation alone cannot determine a critical-work sign.

For a nondegenerate closed geometric triad $k_1+k_2+k_3=0$ with all three full Fourier coefficients nonzero, any nonzero internal full-pair acceleration forces, by reality and the exact pair-silence algebra, a nonzero companion pair at some $k_i-k_j$ with
\[
|k_i-k_j|>\max_\ell |k_\ell|.
\]
There is no universal multiplicative scale gap.  A real one-triad finite-Fourier state can therefore close only by becoming nonlinear-silent.  The earlier helical-only phrase ``only homochiral Beltrami'' is too narrow: non-equilateral closure is common-normal planar shear, while equilateral closure includes the monochromatic $2D3C$ family
\[
u=n\times\nabla\psi+c\rho\psi n,
\qquad -\Delta\psi=\rho^2\psi,
\]
with Beltrami only a subfamily.

At a fixed child, write a helical pair source as $F_e=C_ea_pa_q$ and $\kappa_e=|p|^2+|q|^2$.  On active parents put $r_k=\dot a_k/a_k$.  Then
\[
\dot F_e=(r_p+r_q)F_e.
\]
Thus every nondegenerate minimal persistent cancellation circuit synchronizes the sums $r_p+r_q$.  Unequal-heat cancellation forces genuine nonlinear radial parent work to compensate the viscous heat-rate mismatch; only same-heat cancellation can remain purely phase-driven.  With $\eta_k=n_k/a_k$ and $h_k=(F_N)_k$, the exact logarithmic steering law is
\[
\dot\eta_k=\frac{h_k}{a_k}-\eta_k^2.
\]
Moreover, along the frozen heat orbit the fixed-output source is the vector Laplace transform of its heat-fiber source measure.  Under finite-variation integrability, cancellation that survives a heat interval must vanish fiber by fiber.  Global fiberwise cancellation is an exact nonlinear-silent heat trajectory.

Under the additional explicit hypothesis that overlapping cancellation circuits force the affine synchronization $r_{k,s}=\sigma+i v\cdot k$ across connected helicity sectors, true viscous NS forces a monochromatic fixed shape with nonlinearity equal only to rigid translation; all nonlinear modal works vanish.  This gives a smooth decaying traveling relative equilibrium on a periodic/finite-Fourier setting, while finite-energy $\mathbb R^3$ admits no nonzero exact $L^2$ state supported on one Fourier sphere.  The hypothesis is not known for a general continuum cancellation network.

These statements narrow the physical road further: local active-triad closure, passive unequal-heat cancellation and globally affine synchronized repair are no longer dangerous mechanisms.  In the current mother-law organization, the older same-heat non-affine loophole survives precisely inside the moment-orthogonal $N_\perp$ and its true $F_N/G$ evolution.  The unresolved problem is whether this remainder can repeatedly regenerate the forced neutral-tail sorter $[H/(2S)](\Lambda-\mu)U$ at unbounded UV scales while all exact full-pair and heat-fiber identities remain satisfied.  The detailed derivations are recorded in \texttt{BSVO\_FULL\_STATE\_FRONTIER.md}; the theorem below remains OPEN.

\section{The final exclusion theorem required for an unconditional QED}

The contradiction architecture requested in \eqref{eq:architecture} becomes valid once one has the following additional statement.

\begin{theorem}[Final physical-road exclusion --- required closure statement]\label{thm:requiredExclusion}
Let $u$ be a maximal smooth true three-dimensional incompressible Navier--Stokes solution on $[0,T_*)$ from $u_0\in\mathcal C_{\mathrm{Clay}}$.  Suppose the endpoint branch $Y$ occurs along the canonical UV sequence, with all descendants interpreted only through the exact physical grammar of the canonical core.  Then no infinite or terminal realization of the stock/viscosity/outflow exhaustion tree is compatible with a finite singular endpoint.  Equivalently,
\begin{equation}\label{eq:YbotRequired}
\boxed{Y\Longrightarrow\bot.}
\end{equation}
\end{theorem}

\begin{remark}[Status]
Theorem \ref{thm:requiredExclusion} remains the only missing implication needed to turn the physical-core programme into a completed global-regularity proof.  The dedicated file \texttt{BSVO\_FULL\_STATE\_FRONTIER.md} is now organized from the mother law $N=u_t+\nu\Lambda^2u$: fixed-set force triangles generate the signed-curl action profile, positive criticality forces a kinetic-work-neutral but helicity-hardening single-helicity tail, and the exact decomposition $PN=[H/(2S)](\Lambda-\mu)U+N_\perp$ isolates the forced radial sorter from moment-orthogonal phase/polarization complexity.  Full-pair leakage, heat-fiber rigidity and conditional affine synchronization remain the microscopic guards on that remainder.  These results do not prove \eqref{eq:YbotRequired}; the quartic phase law also forbids replacing the missing theorem by an invented traffic/material bridge.
\end{remark}

\section{Conditional final composition and QED}

The logical composition itself is immediate and completely rigorous once Theorem \ref{thm:requiredExclusion} is available.

\begin{theorem}[Global regularity by final exhaustion --- conditional on Theorem \ref{thm:requiredExclusion}]\label{thm:conditionalQED}
Assume the canonical physical-core identities and endpoint theorems above, and assume in addition the final physical-road exclusion Theorem \ref{thm:requiredExclusion}.  Then every maximal smooth solution from $u_0\in\mathcal C_{\mathrm{Clay}}$ is global:
\[
T_*=\infty.
\]
\end{theorem}

\begin{proof}
Assume for contradiction that
\[
T_*<\infty.
\]
By the endpoint hard-shell gate and the exact last-entry identity,
\[
T_*<\infty\Longrightarrow X\vee Y.
\]
By initial critical-tail vanishing,
\[
X\Longrightarrow\bot.
\]
By Theorem \ref{thm:requiredExclusion},
\[
Y\Longrightarrow\bot.
\]
Therefore
\[
X\vee Y\Longrightarrow\bot,
\]
and hence
\[
\boxed{T_*<\infty\Longrightarrow\bot.}
\]
The assumption of a finite maximal lifespan is impossible.  Thus
\[
\boxed{T_*=\infty.}
\]
The maximal smooth solution is global.  \qedhere
\end{proof}

\section{One-page proof graph}

The entire architecture can be read backward from the desired endpoint contradiction:
\[
\boxed{
\begin{array}{c}
T_*<\infty\\[2mm]
\Downarrow\ \text{critical endpoint rigidity}\\[2mm]
\exists\ t_n\to T_*,\ M_n\to\infty:\ M_nE_{M_n}(t_n)>c_\nu\\[2mm]
\Downarrow\ \text{last-entry Kirchhoff at }\theta=1/5\\[2mm]
X\vee Y\\[2mm]
\swarrow\qquad\searrow\\[-1mm]
X\qquad\qquad Y\\[1mm]
\Downarrow\qquad\quad\Downarrow\\[-1mm]
M_nE_{M_n}(0)>c_\nu/5
\qquad
M_n\Phi_{\mathrm{in},M_n}\ge4c_\nu/5\\[1mm]
\Downarrow\qquad\quad\Downarrow\\[-1mm]
\bot\ \text{ by }\Hdot^{1/2}\text{ tail}
\qquad
G\vee B_{\mathrm{bad}}\\[1mm]
\qquad\qquad\Downarrow\ \text{side recipient / first-contact law}\\[1mm]
\qquad\qquad\text{universal bad-positive sidecar}\\[1mm]
\qquad\qquad\Downarrow\ \text{helicity annulus + Kirchhoff}\\[1mm]
\qquad\qquad S\vee V\vee O\\[1mm]
\qquad\qquad\Downarrow\ \text{owner/recycling/source/representation quotients}\\[1mm]
\qquad\qquad\text{no fourth fate }M\\[1mm]
\qquad\qquad\Downarrow\\[1mm]
\qquad\qquad\boxed{\text{Final exclusion theorem still required for }Y\Rightarrow\bot}
\end{array}
}
\]
The quartic phase commit closes the representation question, while the subsequent full-state identities narrow the surviving line to the explicit `B/S/V/O` recycling block.  They do not yet replace the final leaf-exclusion implication.

\section{Conclusion}

The canonical architecture is exceptionally rigid at two throats:
\begin{enumerate}[label=\arabic*.]
\item finite singularity forces UV hard shells, and exact mode-set continuity gives the exhaustive first alternative
\[
T_*<\infty\Longrightarrow X\vee Y;
\]
\item the initial critical tail eliminates $X$, and the same canonical work law pushes $Y$ to a universal bad sidecar and then to the exact sidecar trichotomy
\[
Y\Longrightarrow S\vee V\vee O,
\]
with no independent fourth fate.
\end{enumerate}
The late commit sequence removes every attempted extra owner or representation escape, culminating in the quartic phase anti-correspondence \eqref{eq:QdotWitness}; the subsequent full-state laws identify the precise surviving mechanism as critical minority-$B$ regeneration coupled to sidecar stock/viscosity/outflow.  What remains is not a representation gap but the exact theorem excluding infinite critical $B/S/V/O$ re-entry.  An unconditional Clay QED requires that theorem, equivalently a proof of $Y\Rightarrow\bot$.  Once such a theorem is supplied, the final logical composition is exactly
\[
T_*<\infty\Rightarrow X\vee Y,
\qquad
X\Rightarrow\bot,
\qquad
Y\Rightarrow\bot,
\]
therefore
\[
T_*<\infty\Rightarrow\bot,
\qquad
\boxed{T_*=\infty}.
\]

\begin{thebibliography}{9}

\bibitem{ClayFefferman}
C. L. Fefferman,
\emph{Existence and Smoothness of the Navier--Stokes Equation},
official problem description for the Clay Mathematics Institute Millennium Prize Problem.
See the official Clay Navier--Stokes problem page:
\url{https://www.claymath.org/millennium/Navier-Stokes-Equation/}.

\bibitem{KenigKoch}
C. E. Kenig and G. S. Koch,
\emph{An alternative approach to regularity for the Navier--Stokes equations in critical spaces},
arXiv:0908.3349; Ann. I. H. Poincar\'e AN 28 (2011), 159--187.

\bibitem{KishimotoYoneda}
N. Kishimoto and T. Yoneda,
\emph{Characterization of three-dimensional Euler flows supported on finitely many Fourier modes},
arXiv:2110.08039.

\bibitem{WangCore}
Canonical Wang--Navier--Stokes physical-core repository,
commit \texttt{5a078873718f6567e51ee7fc064a65ecb205f796},
files \texttt{PHYSICAL\_CORE.md}, \texttt{MIXED\_FRONTIER.md}, and \texttt{README.md}.

\end{thebibliography}

\end{document}
