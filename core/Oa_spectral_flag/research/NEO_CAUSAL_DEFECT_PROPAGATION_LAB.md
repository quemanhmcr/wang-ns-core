# NEO Causal Defect Propagation Lab
## From compulsory producer geometry to terminal incompatibility without inventing a new jet

**Status:** research lab.  No rigidity theorem is claimed.

This lab assumes the curl-genetic core and the provenance compiler.  Its purpose is to attack the following finite seam:
\[
\boxed{
\text{terminal saturated contact}
\leftarrow
\text{finite-lag positive-production owner}
\quad\text{but how does off-involution defect propagate?}
}
\]

The input geometry developed on the Type-I research branch is used here only as a **research input contract**.  This branch does not import uncommitted files or claim them as canonical theorems.

---

## 1. Minimal defect coordinates -- INPUT / EXACT ALGEBRA

On \(\rho=|\omega|>0\), let
\[
n=\omega/\rho,
\qquad
a=n\cdot Sn,
\qquad
b=P_{n^\perp}Sn,
\]
\[
g=\operatorname{tr}(A^2),
\qquad
\delta=6a^2-g.
\]
On C1 with \(g>0\), the canonical Riccati sheet is
\[
\boxed{b=0,\qquad\delta=0.}
\]
The polynomial costumes
\[
\Phi_{\rm ang}=|\omega\times S\omega|^2=\rho^4|b|^2,
\]
\[
\Phi_{\rm gain}=6(\omega\cdot S\omega)^2-g|\omega|^4=\rho^4\delta
\]
are useful for analytic zero sets, while \((b,\delta)\) are the scale-level first-jet coordinates.

**Typing rule.** Use polynomial costumes when zeros must cross \(\rho=0\) analytically; use \((b,\delta)\) only on the curl-active set.

---

## 2. Existing causal input -- RESEARCH INPUT CONTRACT

The Type-I programme currently aims to provide a finite lagged box \(\mathcal B_*\) in the causal past of a saturated contact such that
\[
\mathcal P_\omega
=\omega\cdot S\omega-\nu|\nabla\omega|^2
\ge p_*>0
\]
on a controlled subregion, and a fixed fraction of the producer geometry is quantitatively separated from exact G3 through
\[
|b|^2+|\delta|\ge\varepsilon_*>0
\]
or the stronger negative-\(g\) radial defect alternative.

The missing implication is
\[
\boxed{
\text{finite-lag quantitative defect}
\stackrel{?}{\Longrightarrow}
\text{terminal incompatibility with }b=\delta=0.
}
\]
A finite local defect mass is not an ancient-time wallet and cannot simply be summed over repeated events.

---

## 3. The correct derivative target is the existing defect -- PROGRAM

The next calculation should derive a system
\[
\boxed{
L_U Z=\mathcal A Z+F,
\qquad
Z=(b,\delta),
\qquad
L_U=\partial_t+U\cdot\nabla-\nu\Delta,
}
\]
with every component of \(F\) compiled into already-owned first-jet, square-anchor and Hodge/companion terms.

This is not forbidden jet chasing.  No new terminal species is introduced: the derivative is used only to propagate the already-minimal defect.

A successful derivation must explicitly track commutators created by the moving direction \(n\) and by \(\Delta P_{n^\perp}\).  Writing merely \(L_U(PSn)=P L_U(Sn)\) is illegal.

---

## 4. Scalarization candidates -- EXPERIMENTAL

Candidate scalar renderers should satisfy three requirements:

1. nonnegative or sign-controlled on the relevant C1 region;
2. vanish exactly on the Riccati sheet after the appropriate curl/gain gate;
3. have a parabolic evolution whose bad terms are already owned.

Natural first candidates are
\[
\Phi_1=\rho^{-4}\Phi_{\rm ang}=|b|^2
\]
on a uniformly curl-active bubble,
\[
\Phi_2=|b|^2+c\,\delta_+,
\qquad
\Phi_3=|b|^2+c\,\delta^2,
\]
and polynomial versions such as
\[
\Phi_{\rm poly}=\Phi_{\rm ang}+c\,\Phi_{\rm gain}^2/\rho_*^4
\]
when a lower curl threshold \(\rho\ge\rho_*>0\) is available.

No candidate is preferred before its full evolution and hostile-model audit are computed.

---

## 5. Desired propagation inequalities -- OPEN

Three levels would each count as real progress.

### Level A: scalar supersolution/subsolution

Find \(\Phi\ge0\) with
\[
\boxed{
L_U\Phi +V\Phi\ge cF_{\rm prod}
}
\]
where \(V\) belongs to a class compatible with the terminal propagator and \(F_{\rm prod}\) has a definite lower bound on the compulsory producer bubble.

### Level B: vector system with controlled fundamental matrix

If scalarization destroys sign, prove a system estimate for \(Z=(b,\delta)\):
\[
L_U Z=\mathcal A Z+F
\]
with a forward influence bound preventing a definite \(F\)-bubble from producing exact terminal zero.

### Level C: unique continuation / quantitative propagation of smallness

If the source cannot be signed, show that exact terminal Riccati contact together with quantitative past off-sheet occupancy violates a backward uniqueness, three-cylinder, frequency-function or propagation-of-smallness theorem in the licensed coefficient class.

These are analytic alternatives for the same owner bridge, not three new mechanisms.

---

## 6. Contact-action constraints on the source -- EXACT STRUCTURE

The contact-action prism gives
\[
\alpha=\gamma+\sigma,
\qquad
\beta=\theta+\kappa,
\]
with \(\beta=b/\Omega\).
Thus the angular Riccati defect cannot disappear without being paid by either
\[
\boxed{
\text{material direction turning}\quad\vee\quad\text{square-anchor angular turning}.
}
\]
Likewise a radial gain defect interacts with the allocation between record-time growth and square leakage.

This suggests that the evolution of \((b,\delta)\) should be organized by the contact-action prism before any higher tensor decomposition is introduced.

---

## 7. Hodge term policy -- PROTOCOL

The pressure Hessian must not be differentiated into a new ontology.  Whenever it enters defect evolution, use the gradient/Riccati equation to compile it into the existing alternatives:
\[
\boxed{
\text{first-jet quadratic geometry}
\vee
\text{physical-time action}
\vee
\text{square-anchor action}
\vee
\text{separated affine owner}.
}
\]
If a local finite-Fourier model can tune the Hodge-normal directions while preserving the first jet, that falsifies a purely local pressure-sign barrier and points to propagation/scale ownership rather than to a new pressure descendant.

---

## 8. Adversarial battery -- REQUIRED BEFORE THEOREM PROMOTION

Every candidate propagation statement must be tested against:

1. affine rotating C0 fields, to reject false scalar-flatness conclusions;
2. exact Riccati affine first jets with independent Hodge-normal perturbations;
3. Burgers vortex and Burgers-layer stretch--diffusion balance;
4. finite Fourier states with both helicities and active critical work;
5. producer patches that are positive in \(\mathcal P_\omega\) but arbitrarily arranged spatially inside a finite box;
6. a two-scale core plus affine companion, to expose terms falsely assumed local.

The test need not prove the PDE theorem.  Its job is to kill structurally impossible sign claims before analytic work begins.

---

## 9. Stop conditions

Stop the scalar-barrier programme if one of the following is established:

- every scalar from a stated low-degree compiled alphabet admits a hostile countermodel;
- the exact defect system has unavoidable sign-indefinite Hodge/companion forcing not controlled by the Type-I class;
- a vector/system propagation theorem is demonstrably better typed than scalarization.

At that point the correct next object is a propagation theorem for the same \((b,\delta)\), not a higher local defect.

---

## 10. Concrete deliverable

The next substantive calculation in this lab is:
\[
\boxed{
\text{derive }L_U b\text{ and }L_U\delta
\text{ in a curl-frame form with all commutators explicit}.
}
\]
Then build a computer-assisted hostile-state search for low-degree scalar combinations.

A successful theorem must shorten the terminal normal-form list.  A new identity without propagation value does not count.

---

## 11. Normal forcing has full rank on minimal defect rates -- EXACT ALGEBRA / AUDIT

At a heat-null G3A contact, write the symmetric trace-free correction
\[
Z=-H_0+\nu\Delta A.
\]
The minimal-defect compiler gives
\[
D_tb=P_{n^\perp}Zn,
\qquad
D_t\delta=d\,W_\lambda:Z.
\]
In an ordered G3 frame \(n=e_3\), the two mixed entries \(Z_{13},Z_{23}\) independently control the two components of \(D_tb\), while the diagonal trace-free shape direction \(W_\lambda\) controls \(D_t\delta\).  The audit `audits/neo_g3_defect_rate_rank.py` computes the linear map from the five-dimensional symmetric trace-free space to
\[
(D_tb_1,D_tb_2,D_t\delta)
\]
and finds
\[
\boxed{\operatorname{rank}=3.}
\]

This is an important negative design result.  There is no hidden algebraic dependence reducing the three instantaneous normal defect-creation rates to a one-sign scalar.  Therefore a universal first-order scalar barrier cannot obtain its sign merely from G3 algebra; it must control the Hodge/heat forcing through the terminal class, exploit causal propagation, or use nonlocal/scale ownership.

The rank calculation alone does not claim that every symmetric trace-free \(Z\) is globally realizable by Navier--Stokes.  Realizability is a separate adversarial/PDE question.
