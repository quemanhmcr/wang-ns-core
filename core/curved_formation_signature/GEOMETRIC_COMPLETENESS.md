# Geometric Completeness of the Differential Spectral Signature

This note records the strongest current upgrade of Core 3.

The original spectral-signature theorem established **state completeness** on the canonical smooth periodic physical core:

\[
E_u=[\nabla_u,C]
\quad\Longleftrightarrow\quad
u=u\ \text{modulo the known Killing/Galilean sector}.
\]

The new question is strictly stronger:

> If the differential signature knows the full mother one-form \(E(\cdot)\) and its curvature degree \(K(\cdot,\cdot)=[R,C]\), does it determine the formation connection itself?

The current answer is not an unconditional theorem in the infinite-dimensional Navier--Stokes category.  What is now established is a sharp **conditional algebraic reconstruction mechanism**, together with strong exact, physical, adversarial, metric-covariant, noisy and nonlinear executable evidence.

The central candidate is

\[
\boxed{
(g_\Sigma,C,E,K)
\quad\Longrightarrow\quad
\nabla
\quad\Longrightarrow\quad
T,R,\mathcal J
}
\]

at generic spectral points, with higher covariant degrees resolving additional symmetry at degenerate points.  Viscosity \(\nu\) is not contained in this reversible geometry and remains one scalar dynamical calibration.

---

## 1. State completeness is not geometric completeness

Two different inverse problems must be kept separate.

### 1.1. State inverse problem

For a fixed canonical physical formation core,

\[
\boxed{
E_u\Longrightarrow u
}
\]

modulo Killing/Galilean symmetry on the smooth periodic class.

This uses the mother as a complete state sensor.

### 1.2. Geometry inverse problem

The new inverse problem is

\[
\boxed{
(C,E(\cdot),K(\cdot,\cdot))
\stackrel{?}{\Longrightarrow}
\nabla.
}
\]

This requires **polarized differential data**.  A single snapshot \(E_u\) is not enough to supply a curvature two-form.  In particular,

\[
K(u,u)=0
\]

by antisymmetry.

Therefore no statement in this note should be shortened to

\[
E_u\Longrightarrow\text{the entire background geometry}.
\]

That is not what was tested and is not what is claimed.

---

## 2. Spectral reduction of the connection

Let \(C\) be the signed curl operator in a typed metric spectral frame.  Split the metric connection into its curl-stabilizing and curl-mixing parts:

\[
\boxed{
\nabla=V+B,
\qquad
[V,C]=0.
}
\]

Here:

- \(V\) acts within curl eigensheets;
- \(B\) mixes distinct curl eigensheets.

The mother is

\[
E=[\nabla,C]=[B,C].
\]

Thus \(E\) sees only the off-sheet connection.

For spectral blocks \(i,j\) with eigenvalues \(\lambda_i,\lambda_j\),

\[
E_{ij}=(\lambda_j-\lambda_i)B_{ij}.
\]

Whenever \(\lambda_i\neq\lambda_j\),

\[
\boxed{
B_{ij}=\frac{E_{ij}}{\lambda_j-\lambda_i}.
}
\]

So degree one already reconstructs the complete cross-sheet connection.

The only connection information left after \(E\) is the stabilizer-valued one-form

\[
V\in\Omega^1(\mathfrak h_C),
\qquad
\mathfrak h_C=\{A\in\mathfrak{so}: [A,C]=0\}.
\]

This is the exact inverse problem exposed by the new campaign.

---

## 3. Curvature turns the hidden connection into a Codazzi measurement problem

Define

\[
K=[R,C].
\]

The key structural observation is that, after \(B\) is known from \(E\), \(K\) acts as a measurement of the unknown within-sheet connection \(V\).

In the exact finite metric-Lie / left-invariant setting used by the reconstruction tribunal, write connection matrices

\[
\Gamma_i=B_i+V_i,
\qquad [V_i,C]=0.
\]

The curvature is

\[
R_{ij}
=
[\Gamma_i,\Gamma_j]-\Gamma_{[e_i,e_j]}.
\]

The torsion-free bracket itself is generated by the connection,

\[
[e_i,e_j]
=
\Gamma_i e_j-\Gamma_j e_i.
\]

After applying \([\cdot,C]\):

- every pure \([V_i,V_j]\) term remains in \(\mathfrak h_C\) and is killed;
- every term in which a \(V\)-generated bracket coefficient multiplies another \(V\) is again stabilizer-valued and is killed;
- the surviving terms contain at most one factor of \(V\).

Therefore in this typed algebraic setting,

\[
\boxed{
K=K_B+\mathcal A_{C,E}(V),
}
\]

where \(K_B\) is computable from \(B\) and \(\mathcal A_{C,E}\) is linear in the hidden stabilizer connection.

This is not a numerical approximation.  The corresponding executable superposition residual is at machine precision.

We call

\[
\boxed{
\mathcal A_{C,E}:V\mapsto K-K_B
}
\]

the **Codazzi observability map** of the differential signature.

---

## 4. Conditional reconstruction principle

The exact finite-dimensional algebra gives a clean conditional statement.

### Conditional reconstruction statement

Assume:

1. the metric \(g\) is carried correctly;
2. \(C\) is self-adjoint in that metric and its spectral splitting is typed;
3. \(E=[\nabla,C]\) is known as a one-form;
4. \(K=[R,C]\) is known as a two-form;
5. the candidate connection is metric-compatible and torsion-free in the formation category;
6. the Codazzi observability map \(\mathcal A_{C,E}\) is injective modulo the genuine stabilizer.

Then:

\[
E\Longrightarrow B,
\]

\[
K-K_B=\mathcal A_{C,E}(V)\Longrightarrow V,
\]

hence

\[
\boxed{
(g,C,E,K)\Longrightarrow\nabla.
}
\]

Once \(\nabla\) is known,

\[
[u,v]=\nabla_uv-\nabla_vu,
\]

so the Lie tensor \(T\), full curvature \(R\), Poisson/Riesz operator \(\mathcal J\), and reversible formation geometry are reconstructed.

This is the correct form of the new geometric-completeness claim.

It is **conditional on observability**.  The experiments below show that this condition is generic in a broad finite adversarial campaign but can fail at highly degenerate spectral strata.

---

## 5. Exact metric-Lie reconstruction tribunal

Canonical audit:

```bash
python core/curved_formation_signature/audits/ek_exact_lie_reconstruction.py
```

The audit hides the bracket, \(T\), connection, curvature and Poisson operator.  It exposes only the metric, \(C\), \(E\) and \(K\), then reconstructs the hidden stabilizer connection through the Codazzi system.

Four exact Lie-algebra families were tested:

- \(\mathfrak{so}(3)\oplus\mathfrak{so}(3)\);
- \(\mathfrak{so}(3)\oplus\mathfrak h_3\);
- \(\mathfrak{so}(3)\oplus\mathfrak{se}(2)\);
- \(\mathfrak h_3\oplus\mathfrak{se}(2)\).

Each was randomized by non-bi-invariant metrics over four seeds.

Result:

\[
\boxed{16/16\text{ tested generic cases had full Codazzi rank}.}
\]

The affine-linearity residual was approximately

\[
1.5\times10^{-16}	ext{--}2.5\times10^{-16}.
\]

Worst full-rank connection reconstruction error:

\[
\boxed{3.97\times10^{-15}}.
\]

The reconstructed connection then generated:

- the bracket;
- the full curvature;
- \(\mathcal J\);
- \(\mathcal L_{\nu,u}\) once \(\nu\) was supplied;
- an independently integrated 80-step trajectory.

Worst trajectory mismatch:

\[
\boxed{5.02\times10^{-16}}.
\]

A noise ladder from \(10^{-10}\) to \(10^{-4}\) gave reconstruction slope

\[
\boxed{1.008},
\]

consistent with linearly stable inversion in that regime.

This audit is exact finite metric-Lie evidence.  It is not by itself an infinite-dimensional Navier--Stokes theorem.

---

## 6. Full physical helical tribunal: curvature recovers a coefficient that mother cannot see

Canonical audit:

```bash
python core/curved_formation_signature/audits/physical_helical_resonant_recovery.py
```

This is the most direct physical test of the mechanism.

A Fourier transition

\[
q\longrightarrow q+p
\]

is selected so that

\[
|q+p|=|q|
\]

and the two modes have the same helicity.  Hence the input and output lie on the same signed curl eigensheet.

The corresponding connection coefficient is therefore exactly invisible to \(E\):

\[
\boxed{
E\text{ sees zero in that vertical channel}.}
\]

A second physical Fourier direction is then used to form a cross-sheet curvature loop.  The resulting \(K\) measurement recovers the hidden within-sheet connection amplitude.

Across 80 independent resonant triads:

\[
\text{median relative error}
=
\boxed{9.56\times10^{-16}},
\]

\[
\text{worst relative error}
=
\boxed{3.51\times10^{-14}}.
\]

The noise slope was

\[
\boxed{1.0004}.
\]

This is direct physical evidence for the central geometric mechanism:

\[
\boxed{
E\text{ measures first-order sheet mixing, while }K\text{ can expose within-sheet connection through cross-sheet holonomy.}
}
\]

---

## 7. Adversarial rank phase diagram: the generic statement has singular strata

Canonical audit:

```bash
python core/curved_formation_signature/audits/ek_rank_phase_diagram.py
```

The campaign sweeps:

- 9 exact six-dimensional Lie-algebra families;
- 9 spectral multiplicity patterns;
- 6 randomized metrics per family/pattern.

Non-scalar patterns range from

\[
2+1+1+1+1
\]

through

\[
2+2+2,\quad3+2+1,\quad3+3,\quad4+2,\quad5+1.
\]

For every tested pattern from \(2+1+1+1+1\) through \(4+2\), the Codazzi map was full rank for all tested seeds.

Across all non-scalar family/pattern pairs:

\[
\boxed{68/72}
\]

were full rank for all seeds.

All four persistent failures occurred in the highly degenerate pattern

\[
\boxed{5+1}.
\]

Examples of remaining nullity were:

\[
\mathfrak{so}(3)\oplus\mathfrak{so}(3):2,
\]

\[
\mathfrak{so}(3)\oplus\mathbb R^3:2,
\]

\[
\mathfrak h_3\oplus\mathbb R^3:11,
\]

and an almost-abelian model: \(1\).

This falsifies the universal claim

\[
(C,E,K)\text{ always determines }\nabla.
\]

The surviving statement is **generic, stratified geometric completeness**.

---

## 8. Scalar curl is an exact fully-dark negative control

When

\[
C=\lambda I,
\]

then

\[
E=[\nabla,C]=0,
\qquad
K=[R,C]=0.
\]

The rank sweep correctly leaves every hidden connection direction invisible.

In the six-dimensional controls,

\[
\boxed{90/90}
\]

hidden coefficients remain dark.

Therefore the geometric-completeness mechanism is not a generic commutator trick.  It depends critically on nontrivial curl spectral separation.

---

## 9. Higher covariant degrees are a completion mechanism at degenerate strata

Canonical audits:

```bash
python core/curved_formation_signature/audits/ek_higher_degree_completion.py
python core/curved_formation_signature/audits/ek_maximal_tower_stabilizer.py
```

At the adversarial \(5+1\) multiplicity pattern, additional degrees were tested:

\[
K,\qquad d_\nabla K,\qquad d_\nabla^2K,\ldots
\]

For three families the remaining \(K\)-kernel was destroyed immediately by degree three:

\[
2\to0,
\qquad
2\to0,
\qquad
1\to0.
\]

For the hardest tested family

\[
\mathfrak h_3\oplus\mathbb R^3,
\]

the sequence was

\[
\boxed{11\to9\to6}
\]

through degrees two, three and four.

The maximal exterior tower available in six base dimensions then stabilized at nullity six.

This yields the correct interpretation:

> Higher degrees are not automatically new independent physics at generic points.  They are additional observability channels that can become essential on symmetry-degenerate spectral strata.

---

## 10. Bianchi and Jacobi help, but do not magically guarantee linearized uniqueness

Canonical audits:

```bash
python core/curved_formation_signature/audits/ek_bianchi_integrability_completion.py
python core/curved_formation_signature/audits/ek_cartan_integrability_closure.py
```

A tempting hypothesis was that adding

\[
d_\nabla R=0
\]

or Jacobi would automatically close every remaining \(K\)-kernel.

The experiments falsified that.

In several \(5+1\) cases, Bianchi or Jacobi did close the kernel.  But for

\[
\mathfrak h_3\oplus\mathbb R^3,
\]

second Bianchi left the \(K\)-nullity unchanged at 11, while Jacobi reduced it only to 7.

Even the maximal spectral tower plus Jacobi and Bianchi retained a five-dimensional **linearized** kernel:

\[
\boxed{\operatorname{nullity}=5}.
\]

Therefore Core 3 does not identify Jacobian full rank with geometric completeness.

---

## 11. Singular linearized observability does not imply finite non-uniqueness

Canonical audit:

```bash
python core/curved_formation_signature/audits/ek_nonlinear_singular_observability.py
```

The five-dimensional kernel above was probed nonlinearly.

For random directions \(w\) in that kernel, the full maximal-tower-plus-Jacobi sensor residual was measured along

\[
V(t)=V_0+t w.
\]

Across two seeds and six random directions, the fitted log--log slope was numerically

\[
\boxed{2.0000000000}.
\]

Thus the remaining directions are first-order blind but quadratically visible:

\[
\boxed{
\|\mathcal S(V_0+t w)-\mathcal S(V_0)\|
\sim c(w)t^2.
}
\]

Random finite sphere scans at radii

\[
0.02,\ 0.05,\ 0.1,\ 0.2
\]

found no machine-zero finite collision.

This is not a proof of nonlinear injectivity.  It is strong evidence that at least some high-degeneracy points behave like **singular observable charts**, not like ordinary dark gauge directions.

This distinction is essential:

\[
\boxed{
\text{Jacobian kernel}\neq\text{finite geometric non-uniqueness}.
}
\]

---

## 12. Metric covariance is mandatory

Canonical audit:

```bash
python core/curved_formation_signature/audits/ek_metric_covariant_reconstruction.py
```

The reconstruction was pushed through non-orthogonal coordinate charts with condition numbers

\[
1,10,100,1000.
\]

When the transported metric \(G_\Sigma\) was used to whiten the chart, the same hidden connection was recovered, with errors ranging from machine precision to the expected conditioning loss at \(10^3\).

When the same coordinates were falsely treated as Euclidean, reconstruction failed by order one, with representative errors around

\[
0.20\text{--}0.53.
\]

Therefore the inverse object is not

\[
(C,E,K)
\]

as raw coordinate matrices.  It is

\[
\boxed{
(g_\Sigma,C,E,K)
}
\]

as typed geometric data.

---

## 13. Sparse 28-dimensional stress test and information threshold

Canonical audit:

```bash
python core/curved_formation_signature/audits/ek_28d_sparse_codazzi_recovery.py
```

The canonical 28-dimensional coordinate laboratory has curl multiplicities

\[
2,6,6,6,6,2.
\]

After \(E\) reconstructs the cross-sheet part, the number of remaining within-sheet connection coefficients is

\[
\boxed{1736}.
\]

Only 12 random scalar curvature projections per state pair were supplied, for a total of 4536 measurements.

Sparse LSQR recovered all 1736 hidden coefficients with relative error approximately

\[
\boxed{2.75\times10^{-12}}.
\]

The descendants \(\nabla,T,\mathcal J,\mathcal L\) were reconstructed at roughly

\[
4\times10^{-13}.
\]

The noise slope was

\[
\boxed{1.0002}.
\]

A measurement-density sweep showed a sharp practical threshold:

| random curvature projections per state pair | equations | hidden-connection error |
|---:|---:|---:|
| 2 | 756 | 0.672 |
| 3 | 1134 | 0.508 |
| 4 | 1512 | 0.245 |
| 5 | 1890 | \(4.4\times10^{-10}\) |
| 6 | 2268 | \(1.3\times10^{-10}\) |

The collapse occurs just after the number of independent measurements exceeds the number of hidden coefficients.

This is strong evidence that the curvature degree behaves like a genuine observability system rather than a decorative redundant tensor.

**Scope warning.**  This 28-dimensional object is a coordinate stress laboratory, not a faithful projected Lie category.  The physical helical tribunal is the stronger evidence for the actual Fourier mechanism.

---

## 14. Held-out spectral prediction: reconstruction recovers a generator, not merely a fit

Canonical audit:

```bash
python core/curved_formation_signature/audits/ek_heldout_spectral_prediction.py
```

Only \(E\) and \(K\) were fitted.

The reconstructed connection was then asked to predict spectral readers never used during fitting:

\[
[\nabla,C^2],
\qquad
[\nabla,C^3],
\qquad
[\nabla,e^{0.17C}],
\]

\[
[\nabla,\sin(0.4C)],
\qquad
[\nabla,|C|],
\]

hinge readers, multiple shifted cuts \(H_a\), curvature readers

\[
[R,f(C)],
\]

and a higher Bianchi level \(dK\).

All held-out predictions were at approximately

\[
\boxed{10^{-15}}.
\]

An \(E\)-only negative control failed a held-out curvature reader by

\[
\boxed{0.931}.
\]

The correct interpretation is:

> In the full-rank regime, \(E+K\) reconstructs the connection that generates the tested spectral functional calculus; it does not merely interpolate the training observables.

---

## 15. Viscosity is not part of the reversible geometric reconstruction

Canonical audit:

```bash
python core/curved_formation_signature/audits/ek_minimal_viscosity_calibration.py
```

The data

\[
(g,C,E,K)
\]

determine the tested reversible formation geometry, but not the scalar viscosity \(\nu\).

Two systems with the same geometric data and different viscosity have different formation operators.  A representative operator gap in the tribunal was

\[
\boxed{0.684}.
\]

Once the reversible geometry is reconstructed, however, one generic observed time tangent is enough to fit \(\nu\).  The noise slopes were approximately

\[
1.02\text{--}1.04.
\]

Thus the strongest current architecture is

\[
\boxed{
\text{differential spectral geometry}
+
\text{one dissipative scalar }\nu
=
\text{full tested formation law}.
}
\]

If \(\nu\) is not known a priori, one generic dynamical calibration suffices in the finite tribunal.

---

## 16. What has actually become stronger about Theory 2

The old interpretation was primarily representational:

\[
\text{formation state}
\longleftrightarrow
\text{spectral signature state}.
\]

The new evidence supports a stronger, carefully typed hierarchy:

\[
\boxed{
\textbf{Level A: state completeness}
}
\]

\[
E_u\Longleftrightarrow u.
\]

Then

\[
\boxed{
\textbf{Level B: spectral differential geometry}
}
\]

\[
C
\xrightarrow{d_\nabla}
E
\xrightarrow{d_\nabla}
K=[R,C]
\xrightarrow{d_\nabla}
R\wedge E
\to\cdots.
\]

And now, in generic tested finite geometry,

\[
\boxed{
\textbf{Level C: formation-geometry reconstruction}
}
\]

\[
(g,C,E,K)
\Longrightarrow
\nabla,T,R,\mathcal J.
\]

This is why Core 3 should no longer be described merely as a bridge between two representations.

The better current description is:

\[
\boxed{
\textbf{a curl-spectral differential observability geometry for the formation core.}
}
\]

---

## 17. Strongest current canonical statement

The strongest wording justified by the present corpus is:

> On the canonical physical periodic Navier--Stokes core, the mother is a complete state representation modulo the known Killing sector.  The full differential curl-signature carries a spectral reduction of the formation connection.  In exact finite metric-Lie models and in direct physical helical tests, curvature resolves connection information hidden from the mother.  Whenever the resulting Codazzi observability map has full rank, \((g,C,E,K)\) reconstructs the connection and its formation descendants.  Highly degenerate spectral strata can be linearly singular; higher covariant degrees add observability there, and the hardest tested singular kernel is quadratically visible rather than demonstrably dark.

This is deliberately weaker than saying that a global infinite-dimensional geometric-completeness theorem has been proved.

---

## 18. Open theorem target

The main theorem target opened by this campaign is now precise.

One wants a typed periodic Sobolev/Fréchet statement of the form:

\[
\boxed{
(g_\Sigma,C,E,K,\ldots)
\Longrightarrow
\nabla
\quad\text{modulo the true differential-spectral stabilizer},
}
\]

with:

1. unbounded operator domains stated explicitly;
2. a rigorous spectral-block or double-operator-integral definition of \(\operatorname{ad}_C^{-1}\) off the commutant;
3. a continuum definition and Fredholm/rank analysis of the Codazzi observability operator \(\mathcal A_{C,E}\);
4. a stratification of high-multiplicity spectral degeneracies;
5. a distinction between linearized singularity and nonlinear non-uniqueness;
6. topology/harmonic modes handled without identifying \(\ker C\) with gauge;
7. boundary extensions recast in the correct Hodge/Stokes category;
8. viscosity treated separately as the scalar dissipative calibration.

Until those points are proved, **formation-geometry completeness remains a theorem candidate with unusually strong adversarial evidence**, not a completed continuum theorem.
