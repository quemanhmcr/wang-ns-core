# 05 — Why this is really the final door

Đây là tài liệu quan trọng nhất cho “new-chat memory”.  Nó giải thích **vì sao frontier hiện tại là final trong ontology của Theory 2**, thay vì chỉ là criterion mới nhất trong một loop.

## 1. Finality criterion 1 — complete state đã được giữ

Historical criteria đều là contractions.  Theory 2 giữ full shifted operator family

\[
\{\mathscr O_a\}_{a\in\mathbb R}.
\]

Activity chuyển từ `a=0` sang shifted `a`, từ one helicity sheet sang another, hoặc từ child-side sang parent-side flag đều vẫn nằm trong same complete state.

Vì vậy “refuge migration” không còn được phép tạo mechanism mới.

## 2. Finality criterion 2 — spectral species không tăng khi differentiate

Higher spectral jets là divided-difference/flag tomography của cùng curl spectral calculus.  Higher order tăng jet order và slot arity, **không tạo một spectral ontology mới**.

NEO/compiler principle:

\[
\text{new differential costume}
\Rightarrow
\text{curl/mother/flag parentage}.
\]

Do đó differentiate current rate để sinh “third mother” không thể là conceptual progress; đó là prolongation của cùng obstruction.

## 3. Finality criterion 3 — scalar polar calculus đã algebraically đóng vòng

Identity

\[
\mathcal U\langle\Lambda u,Z_{\rm Cod}\rangle
=(\mathcal UW)'-W^2-\mathcal U\mathcal C_{\rm compat}
\]

chứng minh exact primitive của longitudinal Codazzi trong scalar polar variables.

Coefficient matching cho mọi local `F(\mathcal U,W)` ép

\[
F=\mathcal UW+\phi(\mathcal U),
\]

và derivative sinh `+W^2` coefficient đúng một.  Vậy không có scalar local cross-term family nào “chưa thử” có thể giữ good square.

Đây là một **no-go theorem**, không phải thất bại search.

## 4. Finality criterion 4 — static cancellation geometry đã classify

Equal-heat source-line alignment không tạo continuum refuge vô hình:

- reflection doublet;
- reciprocal singlet;
- Beltrami/shear null seams.

Reciprocal conductance có sharp lower bound và bounded canonical multiplicity.  Vì vậy static “maybe cancellation geometry has one more hidden species” không còn là licensed excuse.

## 5. Finality criterion 5 — kernel của dynamic defect đã classify

Physical parabolic synchronization zero set:

\[
\delta_\Diamond r=0
\Rightarrow
r(k)=\sigma+b\cdot k.
\]

Tức zero defect là symmetry/terminal class, không còn dangerous 3D kernel.

Đây là điều historical scalar criteria thường thiếu: quantity bằng zero nhưng dynamics nguy hiểm vẫn sống.  Ở đây zero set đã đúng loại.

## 6. Finality criterion 6 — current obstruction là rate của existing source, không phải observer

`f_e` đã là physical quadratic source field.  `h_e=\dot f_e` là true derivative của chính field đó.  Projective defect

\[
f_{e'}h_e-f_eh_{e'}
\]

không phải một arbitrary reader; nó là coordinate-free relative motion của source ray.

Ta không “chọn thêm criterion”.  Ta đang hỏi complete nonlinear source **thực sự chuyển direction như thế nào theo thời gian**.

## 7. Finality criterion 7 — mọi shortcut đã bị type-check

### Raw UV amplification

\[
\frac{\|J\|_2^2}{S_2}\to\infty
\]

không scale invariant, nên không tự là coherence.

### Hardy control

`\mathcal H^1` là real Hardy space, không phải Sobolev `H^1`; finite Hardy mass tương thích với infinite `L^2` concentration.

### Moving-flag plateau

All-UV packet có `J_a=J_0` trên một cut interval, nhưng plateau chỉ loại trace-loss; nó không tạo finite `G_{-1/2}` owner.

### Finite incidence

Companion multiplicity không bằng genealogy/reservoir multiplicity.

### Fredholm/operator entropy

Euler congruence để lại weighted stretching term, chỉ là `W` trong coordinate khác.

Mỗi shortcut fail bằng **exact type reason**.  Điều này là bằng chứng rằng frontier không phải do ta “chưa thử đủ scalar tricks”.

## 8. Finality statement được phép viết

Ta được phép viết:

\[
\boxed{
\text{Within the current complete Theory-2/NEO ontology, the only unclosed arrow is dynamic hypocoercive control or terminal degeneration of the physical source-ray rate.}
}
\]

Ta **không** được viết:

\[
\boxed{\text{Navier--Stokes is solved.}}
\]

Finality ở đây là finality của **reduction/ontology**, không phải completed Clay theorem.
