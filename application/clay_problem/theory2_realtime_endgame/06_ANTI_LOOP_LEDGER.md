# 06 — Anti-loop ledger

Bảng này dùng như runtime guardrail.  Nếu new chat đề xuất một route ở cột trái, phải đọc cột phải trước khi đi tiếp.

| Route / apparent key | Status | Vì sao không phải final closure |
|---|---|---|
| Zero-cut traffic | compiled | shifted flags giữ activity zero-cut bỏ mất |
| Convex transport potential | exact reader | danger threshold có thể cross; không finite coercive owner |
| `G_{-1}` heat-fiber forcing | insufficient | thiếu đúng half heat derivative |
| Moving flag | exact repair | supplies half derivative geometrically, nhưng chưa supplies finite time owner |
| Reciprocal A+B | static seam closed | không tự bound dynamic source-ray rate |
| Companion multiplicity | exact finite incidence | không bằng temporal/genealogical reuse multiplicity |
| Codazzi `L_t^2H^{-1}` budget | open/wrong ontology | standalone estimate tạo time-integrability loop |
| Second/third mother | ancestry known | differentiate chỉ prolong same obstruction |
| Scalar `(\mathcal U,W)` cross term | exact no-go | coefficient matching cancels good `W^2` |
| Operator determinant / spectral entropy | no sign | Euler congruence leaves weighted critical stretching |
| Fixed critical mass per generation | no contradiction | kinetic cost decays like `1/K` |
| Deep low--high--high | controlled majorant | positive critical creation `\le V_\rho`, radial kernel gains `(L/K)^2` |
| Raw `||J||^2/S_2` amplification | scaling trap | ratio grows under concentration without extra coherence |
| Hardy `\mathcal H^1` source control | true but too weak | endpoint is Hardy-to-`L^2` UV concentration |
| Finite-window rank audit | evidence only | kernel theorem exact continuum, but quantitative dynamic owner still absent |
| New source-rate norm `||\dot f||` | forbidden shortcut | recreates Codazzi/time-packing owner problem |

## Runtime rule

Một đề xuất mới chỉ được tiếp tục nếu nó:

1. giữ **same final object** `\Omega_{e,e'}` / `\delta_\Diamond r`;
2. dùng only already-owned viscosity/kinetic/flag/A+B structures;
3. hoặc chứng minh degeneration vào existing terminal kernel.

Nếu nó cần đặt tên một new descendant để rồi đi tìm budget cho descendant đó, mark ngay:

\[
\boxed{\text{HISTORICAL LOOP RISK}.}
\]
